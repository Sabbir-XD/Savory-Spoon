import React from 'react';
import BackgroundTitle from '../components/BackgroundTitle';

const Gallery = () => {
    return (
        <div>
            <BackgroundTitle />
        </div>
    );
};

export default Gallery;